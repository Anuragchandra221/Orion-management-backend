# Orion-management-backend

This is the backend code for the Orion management system

clone and cd into the repo
inistall the requirements.txt
pip install -r requirements

run the server
python manage.py runserver
